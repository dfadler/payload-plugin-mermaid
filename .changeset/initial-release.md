---
'payload-plugin-mermaid': major
---

Initial release: a Mermaid diagram block for Payload CMS — server-rendered SVG via zombie-mermaid, a live admin preview, an "Open in Mermaid Live Editor" button, and zero client-side rendering JS shipped to the frontend.
